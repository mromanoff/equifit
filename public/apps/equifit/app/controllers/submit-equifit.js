/*
define(function (require, exports, module) {
    'use strict';

    var app = require('app');
    var SubmitEquifitEntity = require('entities/submit-equifit');
    var SubmitEquifitModule = {};

    var submit = new SubmitEquifitEntity();


    // create an instance of forms collection.
    //var formsEntities = new FormsEntities();

    SubmitEquifitModule.init = function (data) {
        console.log('submit equifit controller', data);
        submit.save(data, {
            wait: true
        });

    };

    module.exports = SubmitEquifitModule;
});*/
